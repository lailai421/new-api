# 集成检查设计

## 检查策略

以父 PRD 验收项建立需求到测试证据映射。优先测试用户可观察契约和安全不变量，不添加随机、sleep、日志-only 或只提高覆盖率的测试。

## 三数据库矩阵

每个数据库记录镜像/服务版本、DSN 隐去凭据后的命令和结果。验证空库迁移两次、从最新发布版代表数据库升级两次、超过 64 KiB 密文、CAS 并发、分页索引、保留清理和删除。

## 泄漏审查

使用静态搜索和可捕获 logger/API 响应测试，检查 Prompt、Token、Guard 请求/响应不会进入日志或非详情响应。前端检查事件列表和 React Query 缓存。

